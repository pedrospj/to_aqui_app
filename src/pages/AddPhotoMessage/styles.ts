import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  selfieImage: {
    height: 300,
    width: 300,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
});

export default styles;
