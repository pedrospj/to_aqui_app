import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    marginTop: '2%',
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  container: {
    marginBottom: '5%',
  },
});

export default styles;
