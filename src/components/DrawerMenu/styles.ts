import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  menuItem: {
    paddingVertical: 10,
    paddingLeft: '10%',
    fontSize: 18,
  },
  logoutContainer: {
    marginLeft: 'auto',
    marginRight: 'auto',
    marginTop: 50,
    color: 'white',
  },
});

export default styles;
